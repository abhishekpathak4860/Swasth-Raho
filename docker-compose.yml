version: "3.8"

services:
  backend:
    build: ./Backend
    ports:
      - "5000:5000"
    env_file:
      - ./Backend/.env

  frontend:
    build: ./Frontend/hospital
    ports:
      - "3000:3000"
    env_file:
      - ./Frontend/hospital/.env.docker
    depends_on:
      - backend